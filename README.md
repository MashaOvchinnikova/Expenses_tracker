# 📊 Expenses Tracker — Учёт расходов через Telegram бот и API

![Python](https://img.shields.io/badge/Python-3.13-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-green)
![Aiogram](https://img.shields.io/badge/Aiogram-red)
![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-orange)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)
![Docker](https://img.shields.io/badge/Docker-✔️-blue)

**Expenses Tracker** — приложение для учёта личных расходов с двумя интерфейсами: Telegram бот и REST API.

## ✨ Возможности

### 🤖 Telegram бот
- `/add 500 еда` — добавление расхода (сумма, категория, описание)
- `/today` — просмотр расходов за текущий день
- `/categories` — список всех категорий с суммами
- `/stats` — статистика по месяцам
- `/help` — список доступных команд

### 🌐 FastAPI
- REST API с автоматической документацией Swagger
- Фильтрация расходов по категориям и датам
- Пагинация для больших списков
- Статистика по категориям

### 🗄 База данных
- PostgreSQL
- SQLAlchemy ORM
- Пул соединений для высокой нагрузки
- Готовые миграции через Alembic

## Используемые технологии
- Python 3.13 — основной язык программирования
- FastAPI — современный веб-фреймворк
- SQLAlchemy — ORM для работы с базами данных
- Alembic — управление миграциями БД
- Pydantic — валидация данных
- Aiogram 3.x — библиотека для Telegram ботов
- PostgreSQL — реляционная база данных
- Docker — контейнеризация приложения
- psycopg2-binary — драйвер PostgreSQL

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.13+
- PostgreSQL 16+
- Docker и Docker Compose (опционально)

### Вариант 1: Локальный запуск

1. **Клонирование репозитория**
````
git clone https://github.com/MashaOvchinnikova/Expenses_tracker.git
cd Expenses_tracker
````
2. **Создание виртуального окружения**
````
python -m venv venv
source venv/bin/activate  # для Linux/Mac
или
venv/Scripts/activate  # для Windows
````
3. **Установка зависимостей**
````
pip install -r requirements.txt
````
5. **Настройка переменных окружения**
````
cp .env.example .env  # Отредактируйте .env, укажите свои значения
````
7. **Создание базы данных PostgreSQL**
````
psql -U postgres
CREATE DATABASE expense_tracker;
\q
````
9. **Запуск FastAPI**
````
uvicorn app.main:app --reload
````
11. **Запуск Telegram бота (в отдельном терминале)**
````
python -m app.bot.bot
````
13. **Открыть в браузере: http://localhost:8000**

### Вариант 2: Запуск через Docker
````
docker-compose up --build
````

## 📖 Документация API
После запуска доступны:

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

## Примеры запросов
### Добавление расхода:
````
curl -X POST http://localhost:8000/api/v1/expenses \
  -H "Content-Type: application/json" \
  -d '{"amount":500,"category":"еда","description":"обед"}'
````
### Получение расходов за сегодня:
````
curl http://localhost:8000/api/v1/expenses/today
````
